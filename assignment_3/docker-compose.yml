version: '3.8'

services:
  app:
    build: .
    container_name: assignment_3_enviroment
    command: python main.py --host 0.0.0.0 --port 8050 --reload
    ports:
      - "8050:8050"
    volumes:
      - .:/app
